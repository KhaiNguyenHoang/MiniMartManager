<UserControl x:Class="MiniMartManager.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniMartManager.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Product Selection -->
        <StackPanel Grid.Column="0" Margin="20">
            <TextBlock Text="Products" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0 0 0 10"/>
            <DataGrid ItemsSource="{Binding AvailableProducts}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectedItem="{Binding SelectedProduct}"
                      Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="Auto"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
                <TextBox materialDesign:HintAssist.Hint="Quantity"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding QuantityToAdd, UpdateSourceTrigger=PropertyChanged}"
                         Width="100" Margin="0 0 10 0"/>
                <Button Content="Add to Cart" Command="{Binding AddToCartCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </StackPanel>

        <!-- Cart -->
        <StackPanel Grid.Column="2" Margin="20">
            <TextBlock Text="Cart" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0 0 0 10"/>
            <DataGrid ItemsSource="{Binding CartItems}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product" Binding="{Binding Product.Name}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice}" Width="Auto"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Text="{Binding TotalAmount, StringFormat=Total: {0:C}}" FontSize="20" FontWeight="Bold" Margin="0 20 0 10" HorizontalAlignment="Right"/>
            <Button Content="Process Sale" Command="{Binding ProcessSaleCommand}" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</UserControl>