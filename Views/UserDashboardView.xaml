<UserControl x:Class="MiniMartManager.Views.UserDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniMartManager.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <materialDesign:Card
            Padding="30"
            UniformCornerRadius="10"
            Effect="{StaticResource MaterialDesignShadowDepth3}"
            Width="400">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon
                    Kind="AccountCircle"
                    Width="80"
                    Height="80"
                    HorizontalAlignment="Center"
                    Foreground="{DynamicResource PrimaryHueMidBrush}"
                    Margin="0 0 0 20"/>
                <TextBlock Text="User Dashboard" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 0 30"/>
                <Button Content="Process Sale" Command="{Binding NavigateToSalesCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Width="250"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>