<UserControl x:Class="MiniMartManager.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MiniMartManager.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Back" Command="{Binding BackCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0 0 10 0"/>
            <TextBlock Text="User Profile" Style="{StaticResource MaterialDesignHeadline4TextBlock}" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Text="User Information" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0 0 0 10"/>
            <TextBlock Text="Username:" Style="{StaticResource MaterialDesignBody1TextBlock}"/>
            <TextBlock Text="{Binding CurrentUser.Username}" Style="{StaticResource MaterialDesignBody2TextBlock}" Margin="0 0 0 20"/>

            <TextBlock Text="Change Password" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0 0 0 10"/>
            <PasswordBox materialDesign:HintAssist.Hint="Old Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 0 0 10"
                         materialDesign:PasswordBoxAssist.Password="{Binding OldPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox materialDesign:HintAssist.Hint="New Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 0 0 10"
                         materialDesign:PasswordBoxAssist.Password="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox materialDesign:HintAssist.Hint="Confirm New Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0 0 0 20"
                         materialDesign:PasswordBoxAssist.Password="{Binding ConfirmNewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Change Password" Command="{Binding ChangePasswordCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
